services:
  - type: web
    name: blueberry-app
    env: node
    buildCommand: npm install && npx vite build
    startCommand: npx tsx server/index.ts
    envVars:
      - key: NODE_ENV
        value: production
